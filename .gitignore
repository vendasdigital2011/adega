# Dependencies
node_modules/

# Next.js build output
.next/
out/
build/

# Environment variables
.env
.env.local
.env.*.local

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor/OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Misc backups (not part of source control)
Adega.rar

# Local workaround script (PATH not refreshed in this session's shell)
dev-with-node.cmd

# Local notes with credentials — never commit
senha subase.txt
*.senha.txt

# Test artifacts (generated, not source)
coverage/
test-results/
playwright-report/
blob-report/
