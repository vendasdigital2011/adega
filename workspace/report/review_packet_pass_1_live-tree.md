# Adega Cloud — Security Review Packet

> **DISCLAIMER:** This report was automatically generated by Mantis AI. All findings and patches are AI-generated and must be manually verified by a security or subject matter expert before deployment or disclosure.

## Target Provenance
- **Target Version:** Git branch `master` at commit `e1ae7db`
- **Snapshot ID:** `live-tree` (pinned: `False`)
- **Pipeline Pass:** `Pass 1`
- **Report Timestamp:** `2026-08-03T17:07:52.523713+00:00`

## Executive Summary

| ID | Title | Risk Score | Priority | Viability | Exposure | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `8210bba5` | Missing Transactional Locking on Concurrent POS Sale Stock Deduction | `6.0` | `HIGH` | `VIABLE` | `EXPOSED` | `VALID` |
| `bc144e18` | Unrestricted Local Mock Store Permission Fallback in Demo Mode | `5.6` | `MEDIUM` | `CONDITIONAL_VIABLE` | `INTERNAL` | `VALID` |

---

## Verified Vulnerability Findings

### Finding 1: Missing Transactional Locking on Concurrent POS Sale Stock Deduction
- **Finding ID:** `8210bba5-a359-404b-8002-e02e5afce2a1`
- **Discovery Snapshot:** `live-tree`
- **CWE:** `CWE-840`
- **Risk Score:** `6.0` / 10.0 (`HIGH`)
- **Production Viability:** `VIABLE`
- **Attacker Position:** `INTERNAL_NETWORK` | **Inferred Exposure:** `EXPOSED`
- **Code Paths:** ``src/services/SaleService.ts:188`, `src/services/SaleService.ts:206``

#### Description & Impact
In `SaleService.create`, stock availability is checked (`p.current_stock >= item.quantity`) and decremented client-side in LocalStorage/IndexedDB mock stores without concurrency locks or atomic state guards. Concurrent sale submissions for low-stock items could result in race conditions.

**Impact:** Business Logic Flaw resulting in negative stock balances during simultaneous POS checkout operations.

#### Validation & Critic Rationale
- **Validation Reasoning:** Verified: SaleService.create checks current_stock and decrements it client-side without row locks or database-level serializable transactions in mock mode.
- **Critic Viability Analysis:** Targeted flaw is fully triggerable in standard production POS checkout operations during high-concurrency periods.
- **Sanity Triage Applied:** `Direct POS Transaction Exposure`

#### Mitigation Strategy
Wrap stock deduction in Supabase RPC database-level row locks (e.g. SELECT FOR UPDATE) or atomic decrements.

#### Reproduction Hints
```text
Simulate 2 concurrent calls to SaleService.create for the last item in stock. Observe both succeed leading to negative stock or unhandled race condition.
```

---

### Finding 2: Unrestricted Local Mock Store Permission Fallback in Demo Mode
- **Finding ID:** `bc144e18-aa3b-4295-9931-0f623c0c6ae6`
- **Discovery Snapshot:** `live-tree`
- **CWE:** `CWE-285`
- **Risk Score:** `5.6` / 10.0 (`MEDIUM`)
- **Production Viability:** `CONDITIONAL_VIABLE`
- **Attacker Position:** `LOCAL` | **Inferred Exposure:** `INTERNAL`
- **Code Paths:** ``src/services/PermissionService.ts:88``

#### Description & Impact
In `PermissionService.listForRole`, when offline or demo mode is active, all permissions are granted by default (`initialGranted = allPerms.map(p => p.id)`). An unauthenticated or low-privileged user accessing local mock stores automatically inherits full administrative privileges across all 14 system modules without server-side validation.

**Impact:** Privilege Escalation to Full System Admin in demo/offline mode.

#### Validation & Critic Rationale
- **Validation Reasoning:** Verified: PermissionService.listForRole returns allPerms.map(p => p.id) unconditionally in offline/demo mode, granting all permissions to any caller.
- **Critic Viability Analysis:** Targeted feature executes during offline or demo mode fallback deployments. Active in staging and offline POS deployments, conditional on demo/offline mode trigger.
- **Sanity Triage Applied:** `Local Attack Vector; Internal Component`

#### Mitigation Strategy
Ensure role-based permission lists in offline/demo fallback strictly reflect defined role scopes rather than granting global access.

#### Reproduction Hints
```text
Set NODE_ENV != test and invoke PermissionService.listForRole('any_role'). Confirm it returns all 112 permission IDs.
```

---

## Appendix: Low Priority Findings
- No low-priority findings recorded in this pass.
